/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using the default .next directory for compatibility with Vercel
  // Add other Next.js config options here
};

module.exports = nextConfig;
